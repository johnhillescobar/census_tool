# Contract Gap Register (Track 2 - Strict Pydantic State Migration)

## Purpose
Track 2 evidence artifact for enforcing strict typed state and deterministic planning contracts.
This register tracks migration from mixed/raw boundaries to strict contract-validated state flow.

Review refresh: 2026-04-26

## Status Legend
- 🟢 enforced: strict typed input/output with validation and deterministic behavior checks
- 🟡 partial: contract shape exists but boundary still allows raw dict/string or non-deterministic fallback
- 🔴 missing: no typed contract enforcement yet

## Gap Table

| Boundary ID | Layer | Boundary | Current Type | Track 2 Expected Type | Status | Deterministic Risk | Track 2 Action | Evidence |
|---|---|---|---|---|---|---|---|---|
| T2-CG-001 | domain | Temporal normalization contract | heuristic parsing still exists in some legacy paths, but Track 2 path now uses typed contract | `TemporalIntent` strict Pydantic model with explicit mode/policy enums | 🟢 | Reduced: typed temporal normalization now fails closed on invalid contract shapes | Keep validation coverage current; tighten any remaining loose mode rules before Track 2 exit if needed | `src/domain/temporal_contract.py`, `src/services/temporal_policy.py`, `app_test_scripts/test_temporal_policy_contract.py` |
| T2-CG-002 | domain | Benchmark normalization contract | implied benchmark semantics replaced on Track 2 path by typed contract | `BenchmarkIntent` strict model with explicit comparison operator + normalization | 🟢 | Reduced: benchmark intent now validates deterministically and `historical_baseline` is fail-closed | Keep `historical_baseline` explicitly deferred or implement typed baseline fields/validators | `src/domain/benchmark_contract.py`, `src/services/benchmark_policy.py`, `app_test_scripts/test_benchmark_contract.py` |
| T2-CG-003 | domain/services | Query plan contract | service boundary emits typed `ComparisonPlan`, and the current planning path now preserves it inside `WorkflowPlanState`; remaining drift risk is downstream where other state/artifact channels stay loose | `ComparisonPlan` preserved directly inside strict Pydantic state models through workflow handoff | 🟡 | The core plan object is now typed across the planning path, but downstream loose channels can still erode guarantees once other artifacts are downgraded to generic dict/list payloads | Keep `ComparisonPlan` as the canonical plan type and finish removing dict/list downgrade points around it | `src/domain/comparison_plan.py`, `src/services/comparison_plan_policy.py`, `src/workflows/comparison.py`, `src/state/types.py`, `app_test_scripts/test_comparison_plan.py`, `app_test_scripts/test_comparison_plan_policy.py`, `app_test_scripts/test_track2_contract_first.py` |
| T2-CG-004 | workflows/state | Node handoff contract | `CensusState.plan`, `artifacts`, and `final` now have typed Pydantic envelopes, and the artifact interior is stronger than the 2026-04-07/2026-04-21 snapshots: `census_data` is now `StrictCensusApiResponse | None`, comparison rows are typed models, and `variable_labels` is typed; however, many other graph channels remain loose and some output/persistence edges still bridge back to dict payloads | strict Pydantic state channels across the graph, with no intra-graph flattening back to generic dicts | 🟡 | Hidden schema drift is reduced on the plan/final/artifact path, but loose non-planning state channels and dict-based adapters can still bypass runtime and future static checks | Extend strict state ownership beyond the current typed envelopes and remove the remaining dict/list downgrade points at output, persistence, and non-planning state boundaries | `src/state/types.py`, `src/workflows/temporal.py`, `src/workflows/benchmark.py`, `src/workflows/comparison.py`, `src/workflows/comparison_metrics.py`, `src/workflows/agent.py`, `src/workflows/output.py`, `app.py` |
| T2-CG-005 | services | Derived comparison math | deterministic service boundary now exists for supported Track 2 derived metrics | deterministic service-only formulas (`difference`, `pct_difference`, `rank`, `percentile`, `trend_gap`) | 🟢 | Reduced: numeric outputs no longer depend on model phrasing for supported metrics | Keep math in service code only and expand workflow-level evidence | `src/services/comparison_metric_compute.py`, `src/workflows/comparison_metrics.py`, `app_test_scripts/test_comparison_metric_compute.py` |
| T2-CG-011 | state | Non-planning state channels | `messages`, `intent`, `geo`, `profile`, `history`, and `cache_index` remain mixed dict/list payloads with implicit shapes | strict Pydantic models for most of `CensusState` with explicit field ownership and validation | 🔴 | Silent key/shape drift outside the planning path can still break routing, memory, and agent/output behavior even if planning contracts are typed | Define strict Pydantic models for most of `CensusState`; add explicit reducers and adapters where needed | `src/state/types.py`, `src/workflows/memory.py`, `src/workflows/agent.py`, `main.py`, `streamlit_app.py` |
| T2-CG-009 | tools | Planning-critical tool typed contract | `strict_census_api_call` is typed, and `validate_geography_params` / `variable_validation` now expose typed request/response contracts with `args_schema`; compatibility shims still accept `str | dict` inputs for LangChain-era callers | planning-critical tools accept typed validated input and return typed validated output | 🟡 | Primary contracts are now typed, but compatibility shims and legacy client wrappers can still reintroduce malformed payloads or loose dict access | Keep typed request/response schemas as the default path, then remove compatibility shims and legacy loose wrappers from planning-critical tool entry points | `src/domain/census_tool_contract.py`, `src/domain/planning_tool_contracts.py`, `src/tools/strict_census_api_tool.py`, `src/tools/geography_validation_tool.py`, `src/tools/variable_validation_tool.py`, `app_test_scripts/test_variable_validation_tool.py`, `app_test_scripts/test_track2_contract_first.py` |
| T2-CG-006 | tests | Canonical temporal/benchmark acceptance | service-level coverage exists, workflow-level canonical suite is still incomplete | canonical suite asserts intent/plan structure + deterministic outcomes | 🟡 | Regressions can still slip through workflow integration boundaries | Add workflow-level canonical cases and gate Track 2 exit on them | `app_test_scripts/test_temporal_policy_contract.py`, `app_test_scripts/test_benchmark_contract.py`, `app_test_scripts/test_comparison_plan.py`, `app_test_scripts/test_e2e_workflows.py` |
| T2-CG-007 | tests | Repeatability guarantee | `ComparisonPlan` rerun assertion exists; temporal/benchmark/workflow rerun coverage is still incomplete | repeated identical input yields identical `TemporalIntent`/`BenchmarkIntent`/`ComparisonPlan` | 🟡 | Nondeterministic planning regressions outside current rerun coverage can still go undetected | Extend rerun assertions to temporal, benchmark, and workflow-level artifacts | `app_test_scripts/test_comparison_plan_policy.py`, `app_test_scripts/test_temporal_policy_contract.py`, `app_test_scripts/test_benchmark_contract.py` |
| T2-CG-008 | governance | Dependency freeze | explicit Track 2 evidence check exists, but the repo now includes `mypy` config plus a dev dependency/lockfile entry that has not yet been reconciled with the freeze policy in the migration evidence | explicit "no dependency change" gate in Track 2 evidence, with any dev-only exception recorded explicitly | 🟡 | Dependency drift is now visible, but the track record is inconsistent until the `mypy` decision is documented explicitly | Reconcile the current `mypy` dev-tooling addition with the Track 2 freeze rule and record the decision before signoff | `migration_evidence/tract2_baseline_20260307/track2_todo_status.md`, `pyproject.toml`, `uv.lock` |
| T2-CG-012 | persistence | Memory schema contract | JSON persistence still uses legacy untyped file shapes for `profile`, `history`, and `cache_index` | versioned strict JSON schema with read-time migration and write-only-new-schema behavior | 🔴 | Persisted state can keep reintroducing invalid shapes even if in-memory models are strict | Add schema versioning, strict persisted models, and explicit migration/adaptation path | `src/workflows/memory.py`, `src/services/memory_utils.py`, `src/clients/file_utils.py`, `memory/` |
| T2-CG-013 | api/output | Output and UI payload contract | the output path is no longer at the baseline state: `src/workflows/output.py` now consumes typed `census_data`, derives a typed `StrictCensusApiRawTable`, passes typed `ChartToolInput` / `TableToolInput`, and normalizes `ChartOutput` / `TableOutput` into `RenderedArtifact`; `src/api/displays.py` and `src/clients/pdf_generator.py` can also coerce typed `FinalResponseState` / `WorkflowArtifactsState`, and the current CLI display test passes. However, Streamlit still assumes legacy dict-style `final`, the tool compatibility shims still accept `str | dict`, LangChain `_run()` / `_arun()` paths still return strings, and `src/workflows/agent.py` still bridges typed `census_data` back to a dict for `footnote_generator` | typed output consumption or explicit adapter layer for external/rendering boundaries | 🟡 | Internal typed state is more protected than before, but public/rendering edges can still drift because the adapter surface is mixed: one path is typed, other callers can still enter through legacy string/dict shims, Streamlit is not contract-aligned, and the footnote path still erodes the typed source of truth | Finish the typed output migration: keep `StrictCensusApiResponse` as the canonical workflow artifact, keep `StrictCensusApiRawTable` as a derived render-only adapter, remove remaining string/dict compatibility shims, migrate Streamlit to the typed state path, replace the footnote dict bridge with a typed adapter/contract, and keep generated-file handling on structured rendered-artifact objects only | `src/workflows/output.py`, `src/domain/rendered_output_contract.py`, `src/tools/chart_tool.py`, `src/tools/table_tool.py`, `src/api/displays.py`, `src/api/__init__.py`, `streamlit_app.py`, `src/clients/pdf_generator.py`, `src/workflows/agent.py`, `src/services/footnote_generator.py`, `app_test_scripts/test_displays.py`, `app_test_scripts/test_pdf_generation.py` |
| T2-CG-010 | governance/tooling | Static boundary type gate | scoped `mypy` configuration now exists for selected Track 2 domain/service modules, but coverage does not yet include the broader state/workflow/tool surfaces and the freeze-policy decision is still undocumented | scoped `mypy` gate for deterministic and state-boundary modules once annotations stop collapsing into `dict[str, Any]` | 🟡 | Static protection is better than the 2026-04-07 baseline, but important state/workflow/output boundaries remain outside the gate and dict-heavy paths still weaken signal | Record the `mypy` freeze decision explicitly, then expand or deliberately limit the gate with clear rationale after additional dict-heavy boundaries are removed | `pyproject.toml`, `uv.lock`, `src/state/types.py`, `src/workflows/`, `src/workflows/memory.py`, `src/workflows/output.py` |

## Track 2 Decision
- Track 2 Step 1 Gate: 🟢 Foundations landed
- Track 2 Overall Gate: 🟡 Partial
- Decision: **Approve with conditions**
- Condition 1 (`t2-state-models`): Replace dict-heavy state channels across most of `CensusState` with strict Pydantic models and explicit reducers/adapters.
- Condition 2 (`t2-boundary-type-preservation`): Stop creating valid Pydantic state objects and immediately dumping them back into generic dict state on the intra-graph path.
- Condition 3 (`tools typed contract`): Keep typed request/response contracts as the default planning path and remove the remaining compatibility shims / loose wrappers from planning-critical tool entry points.
- Condition 4 (`t2-mypy-boundary-gate`): Record the current `mypy` tooling decision under the Track 2 freeze rule, then expand or deliberately bound the static gate while continuing to narrow `Any` usage.
- Condition 5 (`t2-persistence-schema`): Migrate persisted memory JSON to a versioned strict schema with explicit read-time migration and write-only-new-schema behavior.
- Condition 6 (`t2-output-consumers`): Refactor CLI, Streamlit, PDF, and output helpers so they consume typed state or explicit adapters instead of dict semantics.
- Condition 7 (`t2-canonical-suite`): Add workflow-level canonical acceptance coverage before Track 2 exit.
- Condition 8 (`t2-repeatability-tests` hardening): Extend repeatability assertions beyond `ComparisonPlan` to temporal, benchmark, workflow, and persisted typed-state artifacts.
- Condition 9 (`historical_baseline` pending scope): Keep `historical_baseline` explicitly deferred or land typed baseline fields/validators.
- Condition 10 (`t2-freeze-deps` exit gate): Confirm dependency freeze again at final Track 2 signoff, including an explicit decision record for `mypy`.

## Recommended Next Steps
- `t2-state-models`: define strict Pydantic models for most of `CensusState`, including graph, persistence, and output-facing channels.
- `t2-boundary-type-preservation`: preserve validated state artifacts as typed objects; do not flatten them back into generic dicts on the intra-graph path.
- `tools typed contract`: keep the typed planning-tool schemas in place and remove the remaining compatibility shims / legacy loose wrappers from those entry points.
- `t2-mypy-boundary-gate`: reconcile the current `mypy` config/dev dependency with the dependency-freeze rule, then expand or intentionally bound the gate while still reducing `Any`/dict-heavy boundaries.
- `t2-persistence-schema`: define strict persisted models, add schema versioning, and migrate legacy memory files.
- `t2-output-consumers`: refactor CLI, Streamlit, PDF, and output helpers to typed state or explicit adapters.
- `t2-track2-suite-refresh`: update `app_test_scripts/test_track2_contract_first.py` to use `AgentSolveResult` instead of removed `AgentOutput`, then rerun the intended Track 2 focused suite.
- `t2-census-data-source-of-truth`: make `StrictCensusApiResponse` the only accepted intra-graph `census_data` shape, then remove the remaining agent/output compatibility bridges that still normalize legacy payloads after the fact.
- `t2-canonical-suite`: replace placeholder workflow tests in `app_test_scripts/test_e2e_workflows.py` with real Track 2 canonical cases.
- `t2-repeatability-tests`: add repeated-input assertions for temporal resolution, benchmark resolution, and workflow node outputs in addition to `ComparisonPlan`.
- `historical_baseline`: either add explicit baseline contract fields/validators or mark it as intentionally deferred beyond Track 2 exit.

## Exit Check Targets (Track 2)
1. `TemporalIntent`, `BenchmarkIntent`, and `ComparisonPlan` are strict typed contracts.
2. Most of `CensusState` is represented by strict Pydantic models with explicit reducers/adapters for graph use.
3. Workflow nodes pass typed objects only and validated state is not flattened back into generic dict state on the intra-graph path.
4. Derived comparison metrics are deterministic service computations.
5. Planning-critical tools enforce typed input/output contracts and fail closed on validation errors.
6. Persisted memory files use a versioned strict schema with explicit migration from legacy payloads.
7. Output/UI consumers use typed state or explicit adapters rather than implicit dict semantics.
8. A scoped static type gate exists for Track 2 boundary modules, or its deferment is explicitly approved under the dependency-freeze rule.
9. Canonical temporal/benchmark suite passes with repeated-input determinism.
10. Dependency manifest remains unchanged for this track unless a documented tooling exception is approved.
