[pytest]
markers =
    integration: marks tests that hit real external APIs (slow)
    unit: marks fast unit tests with mocks (fast)
    modern_runtime: opt out of classic AGENT_RUNTIME autouse fixture
    slow: long-running tests

